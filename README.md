# Interview content pipeline (static visual)

Self-contained HTML + vendored Mermaid (no CDN) for reliable rendering.

## GitHub Pages

1. Create a new public repository on GitHub and push this folder.
2. Enable **Settings → Pages → Build and deployment → GitHub Actions** (recommended) **or** **Deploy from a branch → `main` → `/docs`**.
3. Open:

`https://<github-username>.github.io/<repo-name>/interview-pipeline-visual.html`

## Production site (ai-automation.studio)

The same files also live in the Next.js project:

`PROJECTS/AI-Automation/aas-landing/public/playbooks/interview-pipeline-visual.html`

After deploy:

`https://ai-automation.studio/playbooks/interview-pipeline-visual.html`
